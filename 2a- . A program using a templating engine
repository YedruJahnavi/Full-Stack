2a- . A program using a templating engine.

npm install express ejs 
const express = require('express'); 
const app = express(); 
const PORT = 3000; 
app.set('view engine', 'ejs'); 
app.get('/', (req, res) => { 
const data = { title: 'Home Page', message: 'Welcome to EJS Example!' }; 
res.render('sample', data); // Render the 'index.ejs' template 
}); 
app.listen(PORT, () => { 
console.log(`Server running at http://localhost:${PORT}`); 
}); 
server.js 
└── views/ 
└── index.ejs 
Create views/index.ejs 
<!DOCTYPE html> 
<html> 
<head> 
<title><%= title %></title> 
</head> 
<body> 
<h1><%= message %></h1> 
<p>This page is rendered using the <b>EJS</b> templating engine.</p> 
</body> 
</html>
